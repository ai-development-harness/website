name: Apply resource hints

on:
  push:
    branches:
      - feat/dynamic-latest-release
    paths:
      - scripts/apply-resource-hints.mjs
      - .github/workflows/apply-resource-hints.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: feat/dynamic-latest-release

      - name: Apply resource hints
        run: node scripts/apply-resource-hints.mjs

      - name: Commit transformed files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "perf: добавить resource hints для загрузки страниц"
          git push origin HEAD:feat/dynamic-latest-release
